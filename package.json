{
  "name": "sauberei-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check script.js && node --check server.mjs && node --check build.mjs",
    "build": "node build.mjs",
    "preview": "node server.mjs dist",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
